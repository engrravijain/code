#include <bits/stdc++.h>
#include "../BigInt.h"

int main () {
    int t; std::cin >> t;
    while (t--) {
        unsigned long long a,b,n; std::cin >> a >> b >> n;
        std::cout << n << std::endl;
    }
    return 0;
}